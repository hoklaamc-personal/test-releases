name: Main Pull Request CI

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize]

jobs:
  required:
    name: Dummy Required Check
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Dummy Required Check Passing"
          exit 0
  optional:
    name: Dummy Optional Check
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Dummy Optional Check Passing"
          exit 0
