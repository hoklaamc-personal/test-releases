name: Main Pull Request CI

on:
  pull_request:
    branches: [main]
    types: [opened]

jobs:
  required:
    name: Dummy Required Check
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Dummy Required Check Failing"
          exit 1
  optional:
    name: Dummy Optional Check
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dummy Optional Check Passing"