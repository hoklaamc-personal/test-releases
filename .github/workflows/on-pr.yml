name: On PR

on:
  pull_request:
    types: [opened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            console.log({ issue: context.issue, owner: context.repo.owner, repo: context.repo.repo });